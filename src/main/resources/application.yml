server:
  port: 8090

spring:
  application:
    name: tearoom
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/tearoom

  jpa:
    database: mysql
    generate-ddl: false
    show-sql: false